# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/portfolio?retryWrites=true&w=majority

# Gmail credentials for email notifications
# Use an App Password (not your real password) — enable 2FA on Gmail first
GMAIL_USER=adityasingh917023@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password

# Where to send notification emails (defaults to GMAIL_USER if not set)
NOTIFY_EMAIL=adityasingh917023@gmail.com

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173

# Server port
PORT=5000
